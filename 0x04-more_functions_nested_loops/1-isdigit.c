#include "main.h"

/**
 * _isdigit - function that checks if a number is digit
 * @c: number to check
 *
 * Return: 1 if c is a digit ,Returns 0 otherwise
 */
int _isdigit(int c)
{
	if ((c >= 0) && (c <= 9))
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
