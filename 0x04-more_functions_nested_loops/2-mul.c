#include "main.h"

/**
 * mul - a function to multiply two integers
 * @a: the first number to multiply
 * @b: the secoond number to multiply
 *
 * Return: result of multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
