#include "main.h"

/**
 * main - a program that print the number of
 * variables it has
 * @argc: numbe rof variables
 * @argv: variables
 * Return: 0;
 */
int main(int argc, char **argv)
{
	printf("%d\n", argc);
	(void)argv;
	return (0);
}
