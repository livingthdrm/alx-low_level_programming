#include "main.h"
/**
 * _abs - returns the absolute value of an integer
 * @int: value to quantify
 *
 * Return: Always 0
 */
int _abs(int)
{
	if (int > 0)
	{
		return (int);
	}
	else if (int == 0)
	{
		return (0);
	}
	else
	{
		return (-int);
	}
	return (0);
}
