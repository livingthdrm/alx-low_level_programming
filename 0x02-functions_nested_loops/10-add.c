#include <stdio.h>
#include "main.h"

/**
 * add - a function to add 2 integers
 * @a: the numbers to add
 * @b: number 2 to add
 *
 * Return: Always 0
 */
int add(int a, int b)
{
	int result = a + b;

	return (result);
}
