#include "main.h"

/**
 * leet - a function that encodes characters into 1337
 * @s: the string to encode
 *
 * Return: the encorded string
 */
char *leet(char *s)
{
	int i = 0;

	while (s[i] != '\0')
	{
		len++;
	}

}
